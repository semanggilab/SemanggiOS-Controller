{
  "name": "semanggi-work-controller",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Semanggi Work Controller — admission control plane over AgentOS/OpenClaw (POC-4).",
  "engines": {
    "bun": ">=1.2.0"
  },
  "scripts": {
    "test": "for f in tests/unit/*.test.mjs; do bun test \"$f\" || exit 1; done",
    "migrate:postgres": "bun run scripts/migrate-sqlite-to-postgres.mjs",
    "start": "bun run src/main.mjs"
  },
  "dependencies": {}
}
